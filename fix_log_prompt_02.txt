All files compile cleanly. Here's a summary of all changes:

**Fix 1 — Unified Sonnet model defaults:**
- `claude_reasoning.py`: imports `SONNET_MODEL` from `pipeline_config` instead of hardcoding `claude-sonnet-4-5-20250929`
- `citation_audit.py`: imports `SONNET_MODEL` from `pipeline_config` instead of hardcoding `claude-sonnet-4-5-20250514`
- `backfill_global_indicators.py`: imports and uses `SONNET_MODEL` instead of inline `os.environ.get` with `claude-sonnet-4-5`
- `seed_projects.py`: same treatment

**Fix 2 — Pricing:** Already correct. $3/$15 per MTok is the current Sonnet 4.6 rate, confirmed via Anthropic docs.

**Fix 3 — Cost cap enforcement in `claude_reasoning.py`:**
- Added module-level `_cumulative_cost_usd` and `_cumulative_tokens` tracking
- Both `reason_with_claude()` and `reason_with_claude_tracked()` now check the cap before calling the API and return `None` when exceeded
- Both accumulate cost after successful calls
- Added `reset_cost_tracker()` and `get_cumulative_cost()` helpers

**Fix 4 — Perplexity disabled:**
- `pipeline_config.py`: `PERPLEXITY_ENABLED = False` (hardcoded, ignores env var)
- Deleted `perplexity_search.py` entirely
- No imports of it existed in other files

**Fix 5 — Cross-reference sector mapping:**
- Added `_XREF_TO_NAICS` dict mapping all 20 abbreviated names (e.g. `"Mining & O&G"`) to full NAICS names from `pipeline_config.NAICS_MAP` (e.g. `"Mining, quarrying, and oil and gas extraction"`)
- `cross_reference_trends()` now translates sector names before looking up in `momentum`, so correlations actually match

**Fix 6 — Enrichment query cap:**
- Changed from `investigations[:50] + detail_fills[:50]` (up to 100) to 60/40 split that never exceeds `MAX_ENRICHMENT_QUERIES_PER_DAY` (55)
